package chapter14

/**
 * 14.7 테스트 조직과 실행
 * 
 * 스칼라테스트에서는 스위트 안에 스위트를 포함시키는 방식을 써서 전체를 조직한다.
 * 어떤 스위트를 실행하면, 그 스위트에 있는 테스트뿐만 아니라 내부에 있는 스위트의 테스트도 실행.
 * 
 * 14.8 결론
 * 
 * 14장에서는 프로덕션 코드 안에 직접 단언문을 추가하는 것과, 외부 단위 테스트에서 이를 사용하는
 * 예를 보았다.
 */